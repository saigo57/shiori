build:
  docker:
    web: Dockerfile.prod
release:
  image: web
  command:
    - ./deployment-tasks.sh
run:
  web: RAILS_ENV=production bundle exec rake assets:precompile & bundle exec puma -C config/puma.rb
